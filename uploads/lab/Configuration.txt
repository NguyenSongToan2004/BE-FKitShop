// Kết nối database
spring.application.name=abc
# URL của cơ sở dữ liệu
spring.datasource.url=jdbc:mysql://localhost:3306/User

# Tên dndawg nhập kết nối cơ sở dữ liệu
spring.datasource.username=root

# Mật khẩu kết nối cơ sở dữ liệu
spring.datasource.password=12345

# Driver class để kết nối cơ sở dữ liệu
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Dialect Hibernate để kết nối sql
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# DDL auto - qu?n l  schema t? ??ng (update, create, create-drop, none)
spring.jpa.hibernate.ddl-auto=update

# Hiển thị SQL trong console (t y ch?n)
spring.jpa.show-sql=true


// Các thư viên: 
 	+ Spring Data JPA
 	+ Spring web
 	+ Spring security
 	+ H2 Database
 	+ Validation
 	+ MySql Driver
 	+ Lombok
	+ Spring security crypto (6.3.3) 	// https://mvnrepository.com/artifact/org.springframework.security/spring-security-crypto\
	+ Mapstruct


// Cấu hình upload file (yaml)
spring:
  servlet:
    multipart:
      enabled: true
      max-file-size: 5MB
      max-request-size: 5MB	



